name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  notify-infra:
    uses: dkta-labs/infra/.github/workflows/notify-release.yml@main
    with:
      service: extract
    secrets: inherit
